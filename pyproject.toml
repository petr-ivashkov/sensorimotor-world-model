[project]
name = "sensorimotor-world-model"
version = "0.1.0"
description = "Sensorimotor world models with a joint forward + inverse-dynamics objective"
requires-python = ">=3.12,<3.13"
dependencies = [
    # toy/ — dot-world / sprite experiments (CPU-friendly)
    "torch>=2.2",
    "numpy>=1.26",
    "pandas>=3.0.1",
    "matplotlib>=3.10.8",
    "pyyaml>=6.0",
    "ipykernel>=7.2.0",
    "jupyter>=1.1.1",
    # planning/ — LeWM-based planning experiments (needs a CUDA GPU to run)
    "stable-worldmodel[train,env]==0.0.6",
    # gymnasium 1.3.0's `all` extra regressed to box2d-py==2.3.5, which has shipped no
    # wheel since cp37 and so needs a system `swig` to build. 1.2.x's `all` pulls
    # box2d==2.3.10, which has cp312 wheels. 1.2.3 is what produced the paper results.
    "gymnasium<1.3",
    "stable-pretraining==0.1.6",
    "gym-pusht==0.1.6",
    "pymunk<7",
    "opencv-python-headless",
    "einops",
    "hydra-core",
    "omegaconf",
    "wandb",
    "h5py",
    "scikit-learn",
    "tqdm",
    "datasets>=2.0,<3",
    "pyarrow<21",
]

[tool.uv]
package = false
override-dependencies = ["opencv-python-headless"]
constraint-dependencies = ["opencv-python-headless"]
