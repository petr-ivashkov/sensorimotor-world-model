executable = run.sh
output = condor.$(Cluster).$(Process).out
error  = condor.$(Cluster).$(Process).err
log    = condor.$(Cluster).log
request_cpus   = 4
request_memory = 122880
request_gpus   = 1
# g164 currently advertises a GPU slot, but PyTorch cannot initialize CUDA there.
requirements   = !regexp("^g164([.]|$)", TARGET.Machine)

queue arguments from (
  config_2_independent.yaml
  config_1_coupled_pair.yaml
  config_1_dot_1_random.yaml
  config_combined.yaml
)
