defaults: [sprite_base]
dataset:
  control: xyt                         # ControlConfig.XYT -> action_dim 3 (x, y, θ all controlled)
  max_delta_theta: 3.141592653589793   # π rad = ±180° turns (overrides matched rim speed ~61°)
output:
  run_name: xyt
