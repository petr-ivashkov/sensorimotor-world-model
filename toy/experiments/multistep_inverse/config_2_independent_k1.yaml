defaults: [dot_base]
dataset:
  groups:
    - {motion: independent, num_dots: 2}
training:
  horizon_k: 1          # inverse head decodes (z_t, z_t+k) -> mean action; k=1 == train.py
output:
  run_name: 2_independent_k1
