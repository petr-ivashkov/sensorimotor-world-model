defaults: [dot_base]
dataset:
  groups:
    - {motion: coupled, num_dots: 2}
training:
  horizon_k_min: 1
  horizon_k_max: 1          # k ~ U{1..1} per example; first-action target; k_max=1 == train.py
output:
  run_name: 1_coupled_pair_kmax1
