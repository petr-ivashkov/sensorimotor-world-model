defaults: [dot_base]
dataset:
  groups:
    - {motion: independent, num_dots: 1}
training:
  horizon_k: 4          # inverse head decodes (z_t, z_t+k) -> mean action; k=1 == train.py
output:
  run_name: single_dot_k4
