# @package _global_

data:
  dataset:
    frameskip: 5
    name: pusht_expert_train
    cache_dir: ${oc.env:EXTERNAL_DATA_ROOT,data/external}
    keys_to_load:
      - pixels
      - action
      - proprio
      - state
    keys_to_cache:
      - action
      - proprio
      - state
