# @package _global_

data:
  dataset:
    frameskip: 5
    name: reacher_train
    cache_dir: ${oc.env:EXTERNAL_DATA_ROOT,data/external}
    keys_to_load:
      - pixels
      - action
      - observation
    keys_to_cache:
      - action
      - observation
